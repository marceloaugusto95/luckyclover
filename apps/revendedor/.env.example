# LuckyClover — Revendedor
#
# The prototype runs entirely on an in-browser mock (src/lib/mockBackend.ts) and needs
# NONE of these variables. They are only required if you wire the app to a real Supabase
# project (see docs/ARCHITECTURE.md §9).

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
